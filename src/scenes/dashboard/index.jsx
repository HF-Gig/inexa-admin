import { Box, Typography } from "@mui/material";

function Dashboard() {
  return (
    <Box>
      <Typography sx={{ fontSize: "24px", fontWeight: "bold", alignItems: "center", justifyContent: "center", display: "flex", height: "100%" }}>
        Dashboard coming soon
      </Typography>
    </Box>
  );
}

export default Dashboard;
